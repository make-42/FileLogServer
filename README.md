# FileLogServer
A simple log server.

## Install
Run ```npm install -g http-server```.

## Running
Execute ```server.bat```.

## Notes
For serving non LAN connections update ```ip``` variable in ```index.html```.
