# FileLogServer
A simple log server.

## Install
Run ```npm install http-server /g```.

## Running
Execute ```server.bat```.

## Notes
For serving non LAN connections update ```ip``` variable in ```index.html```.
